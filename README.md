# Jupyter Notebook using IJava
> docker-compose env to run Jupyter Notebook with IJava

## run

    docker-compose up -d

## connect to [`Jupyter Notebook`](http://localhost:8888)

    http://localhost:8888
